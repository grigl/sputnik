<%= form_for(@activity_photo) do |f| %>
  <% if @activity_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@activity_photo.errors.count, "error") %> prohibited this activity_photo from being saved:</h2>

      <ul>
      <% @activity_photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :activity %><br />
    <%= f.select :activity_id, @activities %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :file %><br />
    <%= f.uploadcare_uploader_field :file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
